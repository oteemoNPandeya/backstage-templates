apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: my-service
  description: A demo service to test Backstage catalog registration
  tags:
    - node
    - backend
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: platform-team
  lifecycle: sandbox
